1、之前axi_stream_insert_header文件中data_out和keep_out信号的赋值我分成了两行，现在我加以修正。同时其中也存在模块端口信号没有对齐的情况，我也加以修正了。

2、乘法的问题我也改进了，使用了向左移动3位来达到乘8的效果。

3、之前的代码在连续的突发传输时会在最后一拍会丢失数据，我重新更换了最后一拍的输出策略。

4、我仿真验证了连续不间断的突发传输的corner case，并且每次突发传输的data_insert、keep_insert，输入数据最后一拍的keep_in信号是随机生成的。最后仿真都得到了正确的结果。
